# 主数据建设记录（T8a）

- 任务：t_22c16495
- 执行日期：2026-08-15
- Base：组织信息库（`FCxObLU6yao5jgsciZfcWHKwnjh`）
- 写入身份：user
- 批次：`IB-T8A-3E86810B7D929F2E`（Base record：`recvslqWd8ryC8`）
- 批次状态：Partial；成功 161，异常日志 14。

## 1. 数据来源与可追溯性

旧原型表均只读，未修改或删除：

- 花名册：`tblK13ENF7v5EwYg`，查询快照 82 行，SHA256 `55678c9fd75d372ce871daac6ed308e34fcd14d49fdba228f3d9b9b2e9ddf276`
- 组织架构表：`tblIeQRH5U7Ip8b5`，145 行，SHA256 `d8b7f8a402f8cf9b4256354d2e47bd83368536bc38df5df80861d83f3e53f4e4`
- 项目渠道检索：`tbl6JGBru8LLWV52`，60 行，SHA256 `c51480c8c2da5b680bedf2a677aa77ef231a7ceea7d03b24c2212ec0123a6e6d`

原始 API 响应存于 `data/raw/`；计划、执行结果与读回核验数据存于 `data/output/`。所有 ID 均由本批计划连续分配，名称只用于源字段归一，写入关联一律为 Base record ID 的 link。

## 2. 拆解和映射规则

1. Organization：组织架构表及花名册的一级部门登记为 Level 1，二级部门登记为 Level 2；组别/部门字段不额外扩成第三级，避免超出 Frozen 模型的一级/二级部门树范围。共 3 个一级、12 个二级，共 15 节点。
2. Position：以 `岗位职务/职务 + 一级部门 + 二级部门` 去重；同名岗位在不同二级部门分别保留定义，不用名称跨部门合并。职级、序列在重复源行中归并为顿号分隔的展示属性。组织架构 145 行与花名册岗位合并后共 53 条。
3. Project：从项目渠道检索的产品序列去重登记，共 4 项。
4. Channel：严格只登记 V60 的 9 个权威渠道；`天猫/淘宝` 作为 `天猫淘宝` 的别名。所有记录 `Is_V60_Authoritative=true`。
5. Employee：姓名仅为展示字段；`Org_ID`、`Position_ID`、`Direct_Manager_ID` 均为 link。花名册中的直属上级名称仅作一次性源解析，解析完成后写入的是 Employee record ID，非名称关联。所有成功记录 `Data_Origin=ROSTER_MOCK`（D-008）。

## 3. 正式表清单与对账

| 实体 | table_id | 已写入 | ID 区间 | 核验 |
|---|---:|---:|---|---|
| Organization | `tblc6rU0d2bHMVnZ` | 15 | ORG000001–ORG000015 | 必填、唯一、父组织 link 全通过 |
| Position | `tbldzvsg9Op6pK29` | 53 | POS000001–POS000053 | 必填、唯一、Org_ID link 全通过 |
| Project | `tbl1GO2vR9ZAqPbr` | 4 | PROJ000001–PROJ000004 | 必填、唯一 |
| Channel | `tblqOGJknsD2H3bt` | 9 | CH000001–CH000009 | 9/9 权威标记 true |
| Employee | `tblc59aB4EnSxkQv` | 80 | EMP000001–EMP000082（EMP000080、EMP000081因对应源行全空未分配） | 必填、唯一、Org/Position link 全通过 |

注：花名册的查询响应为 82 行，但第 80、81 行为所有字段均空的占位记录，不是可识别人员；不能伪造 Name、部门、岗位或入职日期，因此未写入 Employee。该差异已记录 Error_Log，故实际可迁移 Employee 为 80，不是静默跳过。

## 4. 异常日志与未注册项

本批向 `Error_Log` 写入 14 条（ERR000201–ERR000214），全部 `待处理`：

- 2 条：花名册全空占位行导致岗位/人员必填字段缺失。
- 8 条：非 V60 权威渠道未注册：京东私域、快手、快手内容、快手达人、抖音内容、抖音达人、视频号内容、视频号达人。
- 2 条：对应全空源行无法映射 Employee 的 Org_ID/Position_ID。
- 2 条：直属上级无法唯一解析为 Employee_ID，保持 `Direct_Manager_ID` 为空，未猜测关联。

Employee 中已成功设置 70 条 Direct_Manager_ID；其余为空均有来源空值或上述显式异常记录。

## 5. 核验结果

- 5 张表均已非空，读取回数：15 / 53 / 4 / 9 / 80。
- 5 张表主键无重复；所有必要字段无空值。
- Organization 父组织 link、Position→Organization、Employee→Organization/Position、Employee→Employee 的现存链接均零失效。
- Channel 的 `Is_V60_Authoritative=false` 数量为 0。
- Employee `Data_Origin` 值域仅为 `ROSTER_MOCK`。
- Import_Batch 读回：Total_Count=161，Success_Count=161，Fail_Count=14，状态 Partial；与正式写入数及异常日志数一致。
- 第二次执行同一脚本的幂等性核验通过：五张主数据表数量维持 15 / 53 / 4 / 9 / 80，本批 Error_Log 仍为 14 条，未重复追加。

## 6. 可回滚与复跑

- 导入脚本：`scripts/import_master_data_t8.py`。重跑时仅在目标表为空或现有业务 ID 集与计划完全一致时继续；不匹配即停止，防止重复/静默覆盖。
- 回滚脚本：`scripts/rollback_master_data_t8.py`。默认只生成回滚清单；明确执行回滚才使用 `--apply`。已验证默认清单的逆依赖删除顺序为 Error_Log(14) → Employee(80) → Channel(9) → Project(4) → Position(53) → Organization(15) → Import_Batch(1)。
- 回滚与导入计划：`data/output/T8a_import_plan.json`、`data/output/T8a_rollback_manifest.json`。

## 7. 给 T8b 的 ID 清单

- Channel 映射：抖音=CH000001；视频号=CH000002；天猫淘宝=CH000003；拼多多=CH000004；京东POP=CH000005；京东自营=CH000006；达播=CH000007；私域=CH000008；外部分销=CH000009。
- Project：伊利高个子=PROJ000001；伊利骨能=PROJ000002；伊利少年状元=PROJ000003；美赞臣跃高=PROJ000004。
- 关联回填必须以本表注册 ID/record link 为准；不得通过名称创建未注册 ID。
