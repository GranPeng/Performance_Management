# 绩效结果说明书 V03 验证记录

- 验证日期：2026-08-18
- 生成方式：`python3 scripts/generate_result_book.py`（仅读取 Base；不修改任何 Base 记录）
- 输出：`data/output/绩效结果说明书_20260818_V03.xlsx`
- 验证器：`python3 scripts/verify_result_book_v03.py --compare-baseline`
- V04 权威文本 SHA-256：`5342d3dda5097460cba2f4d6e2cb0628c2fbf6e22a5e111cc91369e1a4953250`

## 工作簿结构与追溯验证

生成并验证通过：192 条 `Performance_Result` 对应 192 条录入明细和 192 条输出明细；输出明细的 V04 评分标准原文空值为 0。

工作簿共 5 个 Sheet：

1. 兴趣电商部：20 名员工，122 条结果明细。
2. 制作部：12 名员工，62 条结果明细。
3. 未关联部门：4 名员工，8 条结果明细。该名称是 8 条 Base 结果所关联员工未维护 `Org_ID` 时的明确异常标识，不推测或补造实际部门。
4. 录入明细：全局下钻 Sheet，含 Target / Actual 记录 ID。
5. 输出明细（文本+计算对照）：全局下钻 Sheet，保留 V04 原文、计算说明、`Result记录ID` 与 `结果ID`。

部门 Sheet 的汇总粒度为“期间 + 员工”，即同一员工跨期间的记录分别保留一行，避免跨月结果被静默合并；每行均含“渠道”列，并同屏展示各指标的目标值 / 实际值 / 达成率 / 得分，以及月度总分 / 档位 / 提成金额合计。

## 抽样复算

| 部门 | 期间 | 员工 | 员工ID | 渠道 | 结果明细行数 | 加权得分复算 | 月度总分 | 结论 |
|---|---|---|---|---|---:|---:|---:|---|
| 兴趣电商部 | 2026-07 | 刘宴莎 | EMP000042 | — | 4 | 36.0 | 36 | 一致 |
| 制作部 | 2026-07 | 古旭锋 | EMP000057 | — | 5 | 50.0 | 50 | 一致 |
| 未关联部门 | 2027-02 | T21中控模拟1 | EMP000083 | 抖音 | 1 | 80.0 | 80 | 一致 |

复算口径：在同一“部门 + 期间 + 员工”范围内，对输出明细的数值型“加权得分”求和，与该范围内唯一的“月度总分”核对；同时确认部门 Sheet 存在且仅存在一条对应汇总行，并核对汇总行渠道与输出明细渠道一致。

## 可重跑验证

1. 对首次生成的工作簿逐单元格读取并写入幂等基线：`data/output/绩效结果说明书_20260818_V03_幂等基线.json`。
2. 再次执行生成脚本。
3. 使用验证器重新读取工作簿并与基线比较。
4. 结果：`PASS：重跑前后工作簿单元格值一致`。

工作簿仅包含静态展示值和说明文本，无 Excel 公式单元格，因此不适用公式重算；计算链复核由输出明细的加权得分与月度总分对照完成。

## 异常与数据质量记录（均未静默处理）

1. 32 名员工的 `Employee.Responsible_Channel_IDs` 为空或无法关联到 `Channel`，涉及 184 条结果；渠道列按约定显示“—”。其余 8 条结果已按主数据映射显示“抖音”（7 条）或“抖音、视频号”（1 条）。
2. 68 条结果缺少负责人 `Manual_Score`，保留为“待人工分”，未将公式占位 0 当作业务得分。
3. 152 条结果的 Target 或 Actual 关联缺失/不适用，保留空值及记录 ID，未补造数据。此类均为定性等级型/奖惩制指标（D-021 已定其为人工评分环节，不计入自动计算链可复核范围）。
4. 8 条结果的员工未维护组织关联，汇总到“未关联部门”Sheet；需在 Employee 主数据维护 `Org_ID` 后重跑脚本，届时将自动生成对应实际部门 Sheet。
5. 逐条异常明细位于 `data/output/绩效结果说明书_20260818_V03_异常日志.json`；生成失败时会写入 `data/output/绩效结果说明书_20260818_V03_生成错误日志.json`。
