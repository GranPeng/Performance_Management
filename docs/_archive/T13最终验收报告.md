# T13 最终验收报告

## 复验执行证据

- 已独立复跑 `python3 scripts/verify_calculation_chain.py`：退出码 `0`，状态 `PASS`，`Performance_Result=180`，`difference_rows=0`。
- 同次输出的两次只读幂等读取均为 180 行，`Monthly_Total` 稳定。
- 对 `T12b边界异常执行结果.json` 的 19 个结果 ID 与独立重算报告逐项比对：19/19 均存在，差异数为 0。
- 回滚清单与执行结果的数量一致：Actual 18 条、Performance_Result 19 条、Import_Batch 2 条。

## §20 七项验收

1. 数据模型稳定：不通过。测试计划与实际修正记录不一致，无法作为准确的批次审计依据。
2. KPI 规则确认：不通过。D-010 豁免下提成“照常计算”的验证未覆盖非零提成比例，规则分支的金额计算结果未被有效区分验证。
3. 飞书计算正确：通过（180/180 独立重算一致，差异 0）。
4. Agent 流程稳定：通过（复跑退出码 0；两次读取幂等稳定）。
5. 异常可追踪：不通过。T12b 计划未随后续修正同步，审计追溯链存在矛盾。
6. 结果可人工复核：不通过。四条精确梯度案例的计划人工分与最终修正值不同，人工无法仅依据计划复现当前结果。
7. 文档完整：不通过。D-010 验证结论超出其零比例输入所能证明的范围，T12b 计划未反映已发生的修正。

## 问题清单

### 【阻断】Q-T13-01：T12b 计划与已执行修正记录不一致

- 输入：`data/output/T12b边界异常模拟计划.json` 中 RST000168、RST000171、RST000174、RST000177 对应的 `manual_score` 分别为 50、150、250、550；`data/output/T12b梯度精确边界修正记录.json` 记录同四行已修正为 100、300、500、1100。
- 预期结果：可审计的模拟计划应与最终执行的测试参数一致，或明确关联不可变的初始计划和完整的最终参数快照，使人工可用交付物复现每个精确梯度总分。
- 实际结果：计划仍保留修正前人工分，却同时声称目标总分为 60、80、100、160；修正记录说明首次执行的权重换算错误。计划本身无法复现当前已通过的四条结果。
- 严重程度：阻断。

### 【阻断】Q-T13-02：D-010 豁免提成验证未覆盖非零提成比例

- 输入：`data/output/T12a_D010豁免验证记录.json`：`Project_Run_Days=77`、`Is_Exempt=true`、`Commission_Base=34.152`、`Commission_Ratio=0`、`Commission_Amount=0`。
- 预期结果：应以 `Is_Exempt=true` 且 `Commission_Base>0`、`Commission_Ratio>0` 的用例验证 `Commission_Amount=Commission_Base×Commission_Ratio`，从而能发现豁免分支错误地将提成比例或金额置零的缺陷。
- 实际结果：已验证用例的提成比例为 0，金额自然为 0；即使豁免分支错误地清零提成比例或金额，也会得到相同结果。现有证据不能证明 D-010 所要求的“提成照发”。
- 严重程度：阻断。

## 最终结论

**NOT_ACCEPTED**。虽然全量独立重算、边界结果对账和幂等复验均通过，但上述两项阻断问题导致 §20 的数据模型稳定、异常可追踪、人工复核和文档完整性不满足验收标准。QA 未修改任何脚本、Base 数据、公式或 Error_Log。

## 缺口修复记录（T14，2026-08-17）

### Q-T13-01：T12b 审计链已对齐

- 根因确认：首次 T12b 执行将 LIVE-001 的人工补分误按 0.2 权重反推；Base 实际 `Weight=0.1`，故已在修正记录中将 RST000168/171/174/177 的人工分由 50/150/250/550 修正为 100/300/500/1100。
- 最终、可复现快照：`data/output/T14最终参数快照.json`。该快照含四条 Base record ID、达成率、自动分、最终人工分、权重、最终分、加权分、月度总分、修正前值及修正原因；按 `Final=Auto+Manual`、`Weighted=Final×Weight` 可手工复算，四个总分分别为 60/80/100/160。
- 原 T12b 计划保留为不可变的首次预检证据，不静默覆盖；最终快照明确关联计划、修正记录与执行结果，消除外部审计歧义。

### Q-T13-02：D-010-R1 非零比例豁免验证已补齐

- PO 已将原 D-010 修订为 D-010-R1：仅当前抖音兴趣电商运营岗的新项目豁免期，提成为当前提成基数 `0.003×达成GSV`，跳过梯度档位与考核系数；绩效评分与加权照常计算、但评分不影响该豁免提成。D-010-R2 要求后续将该基数和适用范围配置化，已另建规则设计任务，未把 0.003 写入通用 Base 公式。
- T14 创建独立 `ACTUAL/SIMULATED` 项目、Actual、Performance_Result 与两条 Import_Batch：`Project_Run_Days=44`、`Is_Exempt=true`、GSV/Commission_Base=100、Commission_Ratio=0.003、Commission_Amount=0.3>0；Auto_Score=100、Weighted_Score=30（正常计分）。逐项读回断言全部为真。
- 证据：`data/output/T14_D010非零比例豁免计划.json`、`T14_D010非零比例豁免执行结果.json`；可逆删除清单：`T14_D010非零比例豁免回滚清单.json`。旧口径 0.05 测试现场已按 Source 精确回滚，记录：`T14_D010失败现场回滚记录.json`。

### 复跑结果

- 连续两次真实执行 `python3 scripts/verify_calculation_chain.py` 均退出码 0：`Performance_Result=181`、`difference_rows=0`、两次只读行数均为 181、`Monthly_Total` 稳定、LIVE 梯度仍与 V04 一致。
- 结论：原两项阻断缺口已关闭；新增 D-010-R2 可配置化属于后续规则设计/实施工作，不影响本次已验证的 D-010-R1 当前口径。
