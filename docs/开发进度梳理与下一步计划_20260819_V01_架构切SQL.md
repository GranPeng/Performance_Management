# 开发进度梳理与下一步计划

> 版本：2026-08-19 V01（架构切 SQL）
> 配套：context/decision_log.md（D-022/D-023/D-024）、context/project_context.md §4/§5

## 一、一句话现状

数据层已验收收口，妙搭应用 P1（修复清理）+ P2（计算引擎）完成并上线；2026-08-19 架构拍板转向：**数据主存储切妙搭 PostgreSQL，Base 只做结果/导入数据存储，不再承担维护与计算**（D-023）。

## 二、已完成阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| T1~T27 + T15r~r11 | 数据层建设：Canonical 14 表模型、V04 规则/V60 预算迁入、真实数据 ETL、192 行基线终验 | ✅ ACCEPTED |
| P1a（commit b62e144） | 妙搭应用致命 bug：Select 崩溃、jsonb 形状统一、ID 空间错配 | ✅ |
| P1b（commit 0882dd9） | 中风险修复：防抖、死按钮、硬编码、枚举统一、错误处理、分页 | ✅ |
| P1c（commit 5098907） | 死代码/模板残留清理（-12,336 行）、as-any 收口 | ✅ |
| P2（commit a58c4f8） | TypeScript 计算引擎（server/modules/calc/ 13 文件）：规则读配置不硬编码、POST /api/calc/preview、jest 30/30、**192 行 vs Base 公式 0 差异** | ✅ |
| 线上发布 | https://jv8fym591u8.feishuapp.com/app/app_17cdx8yk8pw （release 7675688094121217314） | ✅ |
| P3a 子卡（代码部分） | 写接口代码完成（commit 3698b68，jest 59/59）：POST /api/actuals 录入、手工评分/复核 PATCH——**但目标是写 Base，需转向** | ⚠️ 待改造 |

## 三、架构决策（2026-08-19，详见 decision_log.md）

- **D-022**：计算引擎 TypeScript 代码化为唯一计算权威，Base 公式字段退役
- **D-023**：系统运行主存储 = 妙搭 PostgreSQL；Base 只存结果/导入数据不维护；**校对核实权威源 = 外部独立标准答案**（V04/V60 Excel、Base 原始录入、192 行基线蓝本），禁止系统自证
- **D-024**：今后新建多维表 Base 禁用关联表功能，表间关系用文本字段存 record_id

## 四、下一步计划（按优先级）

| # | 事项 | 说明 | 前置 |
|---|---|---|---|
| 1 | 看板调整（**待 PO 确认**） | t_a1ea45ca（写 Base 试写验证）关闭作废；P3a（t_39668d19）改目标为写妙搭 SQL；P3b（t_896867d7）重定义=引擎结果落 SQL + 结果导出，原「Base 公式改普通字段」取消 | PO 确认 |
| 2 | P3a 转向实施 | 三个写接口（录入/手工评分/复核）改写妙搭 SQL——应用内 ORM，无凭证卡点，代码骨架已有，工作量小 | #1 |
| 3 | 数据迁移 | Base 14 表数据导入妙搭 SQL；导入后**逐条比对 Base 原始记录**（外部权威源校对，D-023） | #2 |
| 4 | 本地备份 cron | `+db-data-export` 定期导出 CSV 到本地（建议每日凌晨），零依赖可先行 | 无 |
| 5 | 结果导出 | 引擎结果 → 结果书 Excel（复用 generate_result_book.py 逻辑）+ 应用内视图完善 | #3 |
| 6 | P4 节点式组织管理 | React Flow 拖拽维护组织架构/岗位/人员/项目关系（数据层已就绪，写操作依赖 #2） | #2 |
| 7 | P5 agent 报数入口 | 飞书 bot 对话 → 结构化 → 写 SQL，最后一步接入 | #2 #3 |
| 8 | 备份 Base（半自动） | 新建 Base（无关联字段，D-024）作备份仓库，CSV 导出→导入，低频人工触发；全自动写待实测 | #4 |

## 五、遗留问题（非阻塞）

1. 王思伟/潘剑秋团队消耗修正：Actual 编导值仍为个人账号值（应 282224.03/14936.34）——随数据迁移（#3）一并处理
2. 广告投放 GSV 口径待 HR 复核（D-017 确认前不更改）
3. 郭丽娜/陈乾/黄泽威 3 人不入绩效（待 HR 最终名单）
4. 32 名员工 Responsible_Channel_IDs 未维护（花名册在线维护入口）
5. 拆双库搁置：`db-env-create` 需专家模式，开关在妙搭网页后台，CLI 不可开
6. 云视频数据 Channel 分组→渠道映射待做
