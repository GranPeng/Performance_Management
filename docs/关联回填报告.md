# T8b 关联回填与 Project_Member 迁移报告

- 执行日期：2026-08-15
- 原始回填批次：`IB-T8B-REL-20260815-01`
- Import_Batch record_id：`recvslw7f2LYhf`
- 正式 Base：`FCxObLU6yao5jgsciZfcWHKwnjh`
- 最终状态：部分完成（Partial）；D-010 已执行，仍有 78 条真实源数据缺陷待在线维护。

## 1. 输入、决策与可追溯性

- 主数据注册来自 T8a 批次 `IB-T8A-3E86810B7D929F2E`；全部 Link 仅引用其已注册的 Position、Channel、Employee、Project record_id，未新建主数据。
- KPI/梯度规则源为 V04，SHA-256：`5342d3dda5097460cba2f4d6e2cb0628c2fbf6e22a5e111cc91369e1a4953250`。
- 预算目标源为 V60，SHA-256：`65d3c2a6a3e26bce63e196bd80025a3f0a293c05d1fa730da7b5fef0c0eb5943`。
- Project_Member 唯一人工维护来源为只读在线表「在职人员对照项目」`tblB8ujzwz3EJsSv`（101 行，D-009.1）。
- D-010 已定：Target 与 Metric 为通过 `V60_Source_Row ↔ Metric.Budget_Field_Ref` 的间接多对多；Target 不挂单一 `Metric_ID`，不新增关联表。
- D-010 已定：历史 Project_Member 的 `Effective_Start` 留空；同步记录标记为`待在线维护`，不得伪造历史生效日。

## 2. 已完成的确定性关联回填

### 2.1 Metric / Commission_Tier 的 Position_ID

V04 源工作表按一表一岗位的确定性映射回填至 T8a 注册岗位：

- 兴趣电商-运营 → 兴趣电商运营
- 兴趣电商-高级主管 → 兴趣电商运营高级主管
- 兴趣电商-广告投放 → 广告投放
- 兴趣电商-直播中控 → 直播中控/运营助理
- 兴趣电商-主播 → 主播
- 兴趣电商-客服 → 电商客服专员（兴趣电商部门）
- 制作部-编导 → 内容主管/编导
- 制作部-视频剪辑 → 视频剪辑师
- 制作部-摄影师 → 摄影师

读回：Metric `45/45`、Commission_Tier `42/42` 的 `Position_ID` 均为唯一单链接。

### 2.2 Target 的 Channel_ID

按 V60 的「渠道 + 源行 + 期间」还原 Target，映射至 T8a 注册的 9 个权威渠道。

读回：Target `6,156/6,156` 的 `Channel_ID` 均为唯一单链接；未新建任何 Channel。

### 2.3 Target.Metric_ID（D-010 关闭为预期行为）

`Metric_ID` 保持为空，不回填单值：

- D-005/D-006 可以定位「主营业务收入→退货后 GSV」「广告投流费→消耗」的预算行；但它们分别存在 7 个和 2 个岗位 Metric 候选。
- D-010 确认该一对多关系是正常业务语义，而非写入失败；关联由 `V60_Source_Row ↔ Budget_Field_Ref` 间接承载。
- 原 216 条 Target 指标映射 Error_Log 已追加 D-010 依据，并更新为 `Process_Status=已忽略`、`Status=Archived`。

## 3. Project_Member：D-010 占位同步及保留异常

### 3.1 已创建 23 条合规历史占位关系

创建条件是每一行均能唯一映射到现有、Active 的 Employee 和 Project；若有渠道，则必须映射到已注册权威 Channel。写入字段：

- `Employee_ID`、`Project_ID`：正式注册 Link；`Channel_ID`：仅在来源有可映射渠道时写入，空渠道表示整项目。
- `Effective_Start=null`：按 D-010 留空。
- `Allocation_Ratio=null`：来源无值，不默认填充。
- `Is_Primary=false`：来源没有主项目标记，明确作为“未确认”占位，禁止伪造主项目语义。
- `Status=待在线维护`：按 D-010 标记；`Source` 保留旧表 record_id 与该占位说明。

读回：Project_Member `23/23` 条均为上述状态；Employee/Project Link 均单链接，23 条 `Effective_Start` 均为空、`Is_Primary` 均为 false。

### 3.2 未创建 78 条：保留为真实数据质量异常

D-010 仅授权 `Effective_Start` 留空，不授权伪造其他必填关联。源表 101 行中另有 78 行无法满足最小关系完整性，因此不能强行建成不完整 Project_Member：

- Employee 未注册或不在 Active 主数据中：涉及 15 个姓名（含上官、史解伟、吴伟楚、孙博文等）；
- Project 缺失：63 行来源未填写「人员对照项目」；
- Channel 为非权威未注册子渠道：`抖音内容`、`视频号内容`、`视频号达人`。空渠道本身可按模型解释为“整项目”，不构成阻断。

这 78 条 Error_Log 仍保持 `Process_Status=待处理`、`Status=Active`，没有被 D-010 错判关闭。每条保留旧在线表的 record_id 与具体缺失/未注册原因，供唯一人工维护入口修正。

## 4. 批次对账与读回验证

| 项目 | 数量 | 读回结果 |
|---|---:|---|
| Metric Position_ID 回填 | 45 / 45 | 通过，均唯一单链接 |
| Commission_Tier Position_ID 回填 | 42 / 42 | 通过，均唯一单链接 |
| Target Channel_ID 回填 | 6,156 / 6,156 | 通过，均唯一单链接 |
| Target Metric_ID | 0（D-010 预期） | 216 条原日志已关闭；间接多对多保留 |
| Project_Member 创建 | 23 / 101 | 23 合规占位已创建；78 条保留异常 |
| 本批次 Error_Log | 317 | 216 条 Target 已忽略关闭 + 23 条 PM 已解决关闭 + 78 条 PM 待处理 |
| Import_Batch | Total=6,344；Success=6,266；Fail=78；Partial | 已读回核验 |

验证脚本 `scripts/verify_t8b_d010.py` 实测通过；结果文件 `data/output/T8b_d010_verification.json`。

### 4.1 QA 复核（t_c1a9efca）三项问题闭环

QA 子任务复核后发现 3 项问题（2 高 1 中），本根任务逐项闭环如下：

| QA 问题 | 严重度 | 处置 | 证据 |
|---|---|---|---|
| Project_Member 23 条 `Effective_Start=null` 违反 FROZEN §4.6 必填约束 | 高 | 按 §17 变更管理正式裁决：新增决策 **D-011**（Owner: Product Owner，依据 D-010 拍板），模型变更 **CHG-T8B-001**，`Effective_Start` 标注调整为「占位可空/正式必填」 | `context/decision_log.md` D-011；`docs/data_model.md` §4.6 变更记录 |
| 原 `verify_t8b_d010.py` 仅校验 Link 单元素含 id，未校验 id 归属 T8a 注册集合 | 高 | 新增加固脚本 `scripts/verify_t8b_d010_hardened.py`：读取线上 Position(53)/Channel(9)/Employee(80)/Project(4) record_id 全集，逐条校验 Metric/Commission_Tier/Target/Project_Member 全部回填 Link 的归属，**违规数=0** | `data/output/T8b_d010_hardened_verification.json` = PASSED |
| Error_Log 与 101 源记录未证明逐条闭环（仅聚合数） | 中 | 加固脚本按「Source 字段追溯 PM」+「Object_ID 匹配 Active Error_Log」逐条闭环：23 条 PM 追溯 + 78 条 Active Error_Log = 101，两两不相交、并集=源全集，无遗漏无重复 | 同上，`source_closure.partition_issues=[]` |

加固验证全量对账：Metric.Position_ID=45/45、Commission_Tier.Position_ID=42/42、Target.Channel_ID=6,156/6,156、Project_Member=23、Error_Log=317（216 关闭 Target + 23 关闭 PM + 78 Active PM）、Import_Batch=Total 6,344 / Success 6,266 / Fail 78 / Partial，全部一致。

## 5. 可回滚性与产物

- 初始关系回填计划/结果/清单：`data/output/T8b_backfill_plan.json`、`T8b_apply_result.json`、`T8b_rollback_manifest.json`。
- D-010 收尾计划/结果：`data/output/T8b_d010_finalize_plan.json`、`T8b_d010_finalize_result.json`。
- D-010 回滚清单：`data/output/T8b_d010_rollback_manifest.json`；已实际执行默认干跑并通过。
- 收尾脚本：`scripts/t8b_finalize_d010.py`；回滚脚本：`scripts/rollback_t8b_d010.py`。两者均默认不写库，必须显式传 `--apply`。
- 验证脚本：`scripts/verify_t8b_d010.py`，只读、全表分页核验。
- QA 加固验证脚本：`scripts/verify_t8b_d010_hardened.py`（注册 ID 集合逐条归属校验 + 101 源记录逐条闭环），结果 `data/output/T8b_d010_hardened_verification.json`。
- 模型变更：`context/decision_log.md` D-011（CHG-T8B-001）；`docs/data_model.md` §4.6 变更记录。

## 标准 Handoff

## Completed Task
任务名称: T8b-关联回填与 Project_Member：补全 Position_ID/Channel_ID/Metric_ID 并建成员分摊表（D-010 收尾 + QA 三项问题闭环）

## Decision
执行 D-010：Target 不挂单一 Metric_ID，使用现有 `V60_Source_Row ↔ Metric.Budget_Field_Ref` 间接多对多；Project_Member 的 Effective_Start 留空。QA（t_c1a9efca）三项问题按 §17 正式闭环：新增 D-011/CHG-T8B-001 将 Effective_Start 约束调整为「占位可空/正式必填」；新增加固验证脚本逐条校验注册 ID 归属与 101 源记录闭环。

## Change
已验证并保留 Metric 45 条、Commission_Tier 42 条 Position_ID 回填与 Target 6,156 条 Channel_ID 回填；关闭 216 条预期的 Target-Metric 日志；创建 23 条可追溯 Project_Member 历史占位并关闭其日志；批次计数更新为 Success=6,266、Fail=78、Partial。新增 `verify_t8b_d010_hardened.py` 实测 PASSED：注册 ID 集合（Position 53/Channel 9/Employee 80/Project 4）零违规、101 源记录 = 23 PM（Source 追溯）+ 78 Active Error_Log（Object_ID）逐条闭环。文档同步：D-011、data_model.md §4.6 CHG-T8B-001。

## Impact
规则岗位关联、预算渠道关联和 Target-Metric 的间接关系均已闭环；正式库与 FROZEN 模型的字段约束冲突已通过正式变更裁决消除；所有回填值经线上注册 ID 集合逐条校验，杜绝未注册 ID。

## Risk
78 条未完成的 Project_Member 会影响项目组人数和成员归属的完整性；在唯一人工维护入口补齐 Employee、Project 或权威 Channel 后，须依据现有 Active Error_Log 续跑。占位记录的 `Is_Primary=false` 与 `Effective_Start=null` 均不是业务结论，必须由在线维护确认（D-011 已授权占位可空，正式维护后必填）。

## Next Step
业务维护人修正「在职人员对照项目」中 78 条 Active Error_Log 对应的 Employee/Project/Channel 映射；之后 Data Engineer 以该表为唯一来源进行增量同步，并由业务方确认 23 条占位关系的生效日与主项目标识。
